name: Deploy Hugo site to GitHub Pages

on:
  push:
    branches:
      - main  # Trigger deploy on push to main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true  # Required for themes using submodules
          fetch-depth: 0    # Needed for full history and .Lastmod

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.125.7'
          extended: true

      - name: Build site
        run: |
          hugo --minify
          touch public/.nojekyll  # Ensure GitHub Pages disables Jekyll

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          # ✅ We *omit* exclude_assets so that CSS and theme assets are included
